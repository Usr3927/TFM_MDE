select
  id,
  name,
  user_id,
  state,
  cash_journal_id,
  cash_register_id,
  move_id,
  cash_real_transaction,
  cash_real_expected,
  cash_real_difference,
  start_at,
  stop_at
from pos_session;
